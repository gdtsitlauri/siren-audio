# Data Layout

Expected local dataset structure:

- `data/asvspoof2019/`
- `data/fakeavceleb/`
- `data/librispeech/`

SIREN does not auto-download third-party datasets. Use the `siren datasets` command for per-dataset hints and place the extracted content into the paths above.
