# Detection Benchmarks

Place committed AUC, EER, latency, and cross-dataset evaluation tables here.

Current committed benchmark families:

- `librispeech_public/`: authentic-only public baseline exports
- `librispeech_devclean_sample/`: very small real-data smoke benchmark
- future `fakeavceleb_*` and `asvspoof2019_*` runs should be added here when available
